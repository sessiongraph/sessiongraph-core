# Architecture

This document tracks design decisions and implementation notes that don't
belong in the master spec. The canonical reference is
`SESSIONGRAPH_SPEC.md` (v1.0).

For the high-level component diagram, intercept strategy, session
identification rules, the session graph schema, and extraction/injection
protocols, see spec section 2.

## Current implementation status

Week 1 / Task 1: scaffold only. See the README for status.
